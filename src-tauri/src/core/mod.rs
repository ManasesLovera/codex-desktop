// Domain logic (projects, agents, git, pty, worktrees, etc.).
// Business rules and stateful services live here (never in commands directly).
// Frontend never calls these; only through commands layer.
